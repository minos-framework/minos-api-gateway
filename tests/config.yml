rest:
    host: localhost
    port: 55909
    cors:
      enabled: true
    auth:
        enabled: true
        host: localhost
        port: 8082
        method: POST
        path: /token
discovery:
    host: localhost
    port: 5567

