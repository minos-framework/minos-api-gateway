rest:
  host: localhost
  port: 5566
  admin:
      username: test_user
      password: Admin1234
  cors:
    enabled: true
  auth:
      enabled: true
      host: localhost
      port: 55909
      path: /auth
      services:
          - name: token
          - name: credentials
      default: credentials
database:
    dbname: api_gateway_db
    user: minos
    password: min0s
    host: localhost
    port: 5432
discovery:
  host: localhost
  port: 5567

