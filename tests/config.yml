rest:
  host: localhost
  port: 5566
  cors:
    enabled: true
  auth:
      enabled: true
      host: localhost
      port: 55909
      method: POST
      path: /auth/validate-token
discovery:
  host: localhost
  port: 5567

