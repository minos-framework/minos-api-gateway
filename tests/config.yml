rest:
  host: localhost
  port: 5566
  cors:
    enabled: true
  auth:
      enabled: true
      host: localhost
      port: 55909
      path: /auth
      services:
          - name: token
          - name: credentials
      default: token
      endpoints:
          - url: "*://*/merchants/*"
            methods:
          - url: "*://*/categories/*"
            methods:
                - GET
          - url: "*://test_2/*"
            methods:
                - GET
                - PUT
                - POST
          - url: "*://test_1/order*"
            methods:
                - GET
discovery:
  host: localhost
  port: 5567

